# Watermelon Code Bot

---

## Setup

```shell
pipenv shell

pipenv install

pipenv install --dev
```

Read the .env.example and rename to .env and changes the values

### Run the bot

```shell

pipenv run start

```
